import * as THREE from "three";

export const roomPosition = new THREE.Vector3(-1.5, 0, -1.5);
export const roomRotation = new THREE.Euler(0, 0, 0);
export const roomScale = new THREE.Vector3(7, 2.5, 3.5);

export const roomTileSize = 0.5;
export const roomFloorThickness = 0.05;

export const debug = true;
